AGENTS.md
packages/runner-protocol/src/gen/
# Gradle owns generated build reports and copied resources.
**/build/
# pnpm owns the generated lockfile; frozen installs validate it in CI.
pnpm-lock.yaml
